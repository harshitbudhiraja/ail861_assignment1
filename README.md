# Run the demo to test everything
python demo.py

# Train the model
python train.py

# Generate text with a trained model
python infer.py --checkpoint outputs/checkpoint_best.pth --prompt "once upon a time"

# Or use main.py directly with custom config
python main.py --config hyperparams.yaml# ail861_assignment1
# ail861_assignment1
